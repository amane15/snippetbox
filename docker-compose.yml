version: '3.1'

services:
  mysql:
    image: mysql:latest
    container_name: mysql_container
    environment:
      MYSQL_ROOT_PASSWORD:  root
      MYSQL_USER: root
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:./db_data
    restart: always


volumes:
  mysql_data:
